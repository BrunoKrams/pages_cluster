apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: react-app
spec:
  ingressClassName: traefik
  rules:
    - host: brunokrams.de
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: react-app
                port:
                  number: 80
    - host: www.brunokrams.de
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: react-app
                port:
                  number: 80
